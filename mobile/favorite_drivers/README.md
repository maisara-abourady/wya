# mobile/favorite_drivers

User-owned roster of drivers a user has marked as favorites; supports add, remove, list, and reorder.

## Public surface
See `MANIFEST.yaml` → `exports:`. The surface file `lib/favorite_drivers.dart` is the only legal cross-BC import target.

## Invariants
See `MANIFEST.yaml` → `invariants:`.

## Failure modes
See `MANIFEST.yaml` → `failure_modes:`.

## Tests
See `MANIFEST.yaml` → `tests:`.

## Evolution
Decisions about this BC's shape are recorded under `.claude/evolution-log/`.
